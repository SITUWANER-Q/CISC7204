services:
  - type: web
    name: macau-tech-analysis
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT macau_tech_analysis:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
